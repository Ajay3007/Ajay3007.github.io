---
/** Ordered procedure. Replaces the hand-numbered lab-step / sn markup (~950
    uses), where the number was typed into the HTML by hand. */
---
<ol class="my-6 list-none space-y-4 counter-reset-[step] [counter-reset:step] p-0">
  <slot />
</ol>
